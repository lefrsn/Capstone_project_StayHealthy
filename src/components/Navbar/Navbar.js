// Navbar functionality
document.addEventListener('DOMContentLoaded', () => {
    // Navbar initialization code can go here
    // Currently the navbar is static and doesn't require JS functionality
    console.log('Navbar component loaded');
});
